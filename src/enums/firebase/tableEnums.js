const keymirror = require('keymirror');

/**
 * Enums for the tables associated to the Firebase services
 * @return {object}   An object who's value mirror the key
 */
module.exports = keymirror({
    users: null
});
